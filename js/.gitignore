# ================
# REVISIT: Look into how the files added by the build process
# to these node_modules/ and bindings/ directories
# can go into the build folder as done with the other
# intermediate files generated by the build process
#
# This would de-clutter the source areas and keep all files
# generated by the build together.  
# This .gitignore file could then be deleted.
# ================
node_modules/
bindings/
usdviewweb/
**/.reports/
**/.results/
**/.cache/
tests-playwright/.data/

# Secrets

.env